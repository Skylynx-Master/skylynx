"""Addresses app."""

# Django
from django.apps import AppConfig


class AddressesAppConfig(AppConfig):
    """Addresses app config."""

    name = 'lahause.addresses'
    verbose_name = 'Addresses'